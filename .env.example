# Frontend environment variables — set these in Railway's frontend service Variables tab.
# Do NOT prefix with quotes. Do NOT commit real values to git.

# Required: full URL of your Railway Django backend API (no trailing slash)
VITE_API_BASE_URL=https://agri-app-production-4d98.up.railway.app/api
